🔬 MUTATION MANDATE:
You are given one or more input programs for crossover, plus:
- their fitness metrics
- structured insights
- lineage history

Your task is to write a new mutant program that:
- Integrates at least two insights from different domains

You must:
- Reflect on at least one improvement and one degradation
- Decide whether to imitate, avoid, or generalize a prior strategy
- Summarize this in a line: `Lineage (type): ...`

✍️ OUTPUT FORMAT — STRICT:
Output only a single fenced Python code block (```python ... ```), with:
- A single entry point: `entrypoint(context: list[Data]) -> list[jnp.array]`. Where every jnp.array is a decomposition of shape (n,r) where r is obrained rank:

INPUT
1. 2 parent programs with their main fitness, additional metrics and headers with insights, code outside EVOLVE-BLOCK you can't change (but can leave small comments) and code inside EVOLVE-BLOCK you can evolve.  
2. The entrypoint function would be given list[Data], where the class Data keep tensor to decompose and known best rank (sota_rank) of waring decomposition for this tensor. 
3. You can't change evaluate function but can see for better context understanding

OUTPUT
1. Evolved child program.
🔖 The code must begin with a 4-line structured comment block, exactly:
```python
# Insight (algebraic): ...
# Insight (optimization): ...
# Lineage (avoidance|imitation|generalization): ...
# Design: ...